import FluidSpatialHero from "@/components/ui/fluid-spatial-hero";

// The landing hero: a canvas of floating product imagery behind a liquid-glass
// prompt box, adapted to the Shopper palette and wordmark. Swap the floating
// images by passing an `images` prop to FluidSpatialHero (see its FloatingImage
// type); the defaults are curated Unsplash product shots.
export function HeroSection() {
  return <FluidSpatialHero />;
}
