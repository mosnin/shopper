import { AgentHero } from "@/components/sections/agent-hero";

// The landing hero: outcome-first for the agent operator and reseller. It
// leads with the live hunt moment, then the one-line MCP connect and the
// proof row (52 tools, Radar 24/7, x402 self-pay).
export function HeroSection() {
  return <AgentHero />;
}
